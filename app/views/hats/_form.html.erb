<%= form_with(model: hat, local: true) do |form| %>
  <% if hat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hat.errors.count, "error") %> prohibited this hat from being saved:</h2>

      <ul>
      <% hat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :style %>
    <%= form.text_field :style %>
  </div>

  <div class="field">
    <%= form.label :material %>
    <%= form.text_field :material %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
